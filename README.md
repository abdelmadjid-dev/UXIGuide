# Project Process Followup

> NOTE: this section will be replaced by a proper README.md file defining the project. For now, it is a communication field between management and development

- [x] Define Project Setup and Local Proof
each directory (`backend` and `script`) define their own `README.md` files with a better description of how each component is structured. Local Proof wise: the script could be built and tried in an `index.html` (no function), and the backend is executed via `adk web` but no agent/subagent is defined, only one socket is define that does nothing.
- [x] Define Screenshot taking and redaction on `script`
- [x] Define basic Live API implementation with initial linkup with frontend (sending images, sending/receives texts and audio) + added a test scenario via index.html